import { publicRequest,userRequest } from "./requestMethods";


const ReqestReducer = {
     _uploadProductAssets : async() => {
         try{
            const res = await publicRequest.post('auth/admin/login', )
         }catch(err){
           
         }
     } 
}